# voiceDeck: бриф на дизайн приложения

Дата: 2026-09-23. Статус: ждёт владельца. Нужно подтвердить порядок экранов, состав первого экрана, оформление самого сервиса и выбрать направление. Состав подтверждён владельцем: нет.

Порядок работы по правилам для интерфейсов: требования, состав, подтверждённый владельцем, бриф, 2–4 направления на выбор, макет, независимая проверка, код. Один экран за раз.

## Цель

Автор загружает корпоративный pptx, видит, как из него собирается дизайн-система, и дальше по этой системе получает презентацию по брифу или слайды вслед за своей речью.

Слова владельца (2026-09-20): «аналог opendesign и claude design, где сначала создается дизайн система и на основе нее генерируются презентации из текста либо из одной идеи». Замечание владельца (2026-09-23): «нет функционала создания дизайн систему из шаблона презентации».

## Что сейчас не так

Проверено 2026-09-23 в браузере и по коду интерфейса.

| Что | Как сейчас |
|---|---|
| Создание дизайн-системы | отдельного шага нет: в навигации вкладки «Колода», «Сцена», «Речь»; загрузка pptx спрятана в выборе шаблона на «Колоде» и на странице, куда ведёт только адрес `#/template` |
| Ход разбора | импорт идёт 1–2 минуты, экран всё это время молчит и потом сразу показывает результат |
| Имя системы | слаг имени файла (`vk-workspace-klientskaya-konferenciya-shablon-03`), переименовать нельзя |
| Палитра | список из 20 цветов, у части роль «other», у шести одинаковая «text_muted»: системы в этом не видно |
| Слайды-образцы | у большинства вместо картинки серая заглушка; образец нельзя исключить из вёрстки |
| Выбор системы для работы | на «Колоде» под кнопкой «Сменить», на «Сцене» выпадающим списком слагов |
| Вкладка «Речь» | прежняя рабочая страница живого режима, дублирует «Сцену» |

## Роли

- Автор: продакт, аналитик, руководитель проекта. Есть корпоративный pptx и текст или идея, нужна презентация в этом стиле.
- Спикер: тот же человек на выступлении. Говорит, слайды идут на экран зала.

## Истории

Истории И-2…И-7 (бриф, три варианта, аудит, файлы, прослеживаемость, живой режим) остаются из `docs/design/ui-requirements.md`. История про шаблон переписана под дизайн-систему.

**И-1. Дизайн-система.** Как автор, я создаю дизайн-систему из pptx, чтобы дальше колода и живые слайды собирались в его стиле без моих правок.

- Создание начинается с первого экрана приложения одним действием.
- Во время разбора видно, что уже готово: паспорт файла, палитра, шрифты, шкала кеглей, поля, слайды-образцы. Остальные части помечены как идущие.
- После разбора видно, как сервис понял файл: цвета с ролями, шрифт и встроен ли он в файл, образцы картинками с типом и назначением.
- Видно, какие образцы в вёрстку не пойдут и почему (держатся на фото, которого нет).
- Систему можно назвать своим именем. Это имя видно в выборе на «Колоде» и на «Сцене».
- Из системы одним действием переходишь к колоде или к сцене, система уже выбрана.

## Карта приложения

| Раздел | Что в нём | Статус |
|---|---|---|
| Дизайн-системы | список систем, создание из pptx, страница системы | новый, первый экран брифа |
| Колода | бриф, ход работы, три варианта, аудит, файлы | прототип работает, бриф после первого экрана |
| Сцена | пульт спикера: выбор системы, запись, текущий слайд и черновик, окно «Зал» | прототип работает, бриф третьим |

Вкладка «Речь» из навигации уходит, её работа есть на «Сцене». Решает владелец.

## Экран 1. Дизайн-система

### Что человек решает на экране

| Решение | Чем видно, что решение принято |
|---|---|
| Годится ли этот pptx как основа: сервис понял палитру, шрифты и образцы так, как задумал дизайнер шаблона | человек нажимает «Использовать» или удаляет систему |
| Какие образцы не пускать в вёрстку (предложение, ждёт подтверждения) | образец помечен «не используется», в колодах его больше нет |
| Как назвать систему | имя в списке систем и в выборе на «Колоде» и «Сцене» |
| Чем собирать дальше: колодой или сценой | переход на выбранный экран с этой системой |

### Путь пользователя

| Этап | Что делает | Что видит | Где спотыкается сейчас |
|---|---|---|---|
| Найти | открывает приложение | список своих систем и действие «Создать» | раздела в навигации нет |
| Создать | выбирает pptx | страница системы заполняется по частям | 1–2 минуты тишины |
| Проверить | смотрит палитру, шрифты, образцы | цвета с ролями, шрифт со статусом, образцы картинками | роли «other», серые заглушки вместо образцов |
| Поправить | называет систему, снимает ненужные образцы | имя, отметки у образцов | ни того, ни другого нет |
| Использовать | переходит к колоде или сцене | выбранная система на месте | выбор системы на других экранах по слагу |

### Состав на подтверждение

1. Слева список дизайн-систем: миниатюра титульного слайда и имя; сверху действие «Создать».
2. В шапке страницы системы: имя (правится по клику), исходный файл, размер кадра, число образцов.
3. Палитра по ролям (фон, поверхность, текст, приглушённый, акцент, второй акцент): по одному образцу цвета на роль с кодом, остальные цвета свёрнуты.
4. Шрифты: семейство, где применяется, встроен в файл или подставлен.
5. Шкала кеглей: ступени с ролями, каждая набрана своим кеглем.
6. Поля и сетка: схема кадра с полями.
7. Слайды-образцы сеткой картинок: тип, назначение в подсказке, метка «не используется» с причиной.
8. Действия страницы: значки «Колода» и «Сцена» с подсказками, что по этой системе соберётся колода или пойдут слайды из речи; удаление в меню.
9. Пока идёт разбор: готовые части уже на месте, у остальных заглушка и шаг («модель описывает образцы: 12 из 29»).

### Пять состояний

| Состояние | Что на экране |
|---|---|
| Загрузка | части, готовые к этой секунде, и шаг разбора у остальных |
| Пусто | систем нет: заголовок и одно действие «Создать из pptx» |
| Ошибка | что случилось и что сделать: файл не pptx, файл повреждён, модель не загружена (описание образцов пропущено, остальное готово) |
| Заполнено | страница системы целиком |
| Край | 55 образцов, длинное имя файла, шрифт, который из файла не извлекается (сжат в EOT) |

### Стандарт

- NN/g, 10 эвристик: статус системы виден всегда, ошибки простыми словами с выходом ([nngroup.com/articles/ten-usability-heuristics](https://www.nngroup.com/articles/ten-usability-heuristics/)).
- NN/g, прогрессивное раскрытие, не больше двух уровней ([nngroup.com/articles/progressive-disclosure](https://www.nngroup.com/articles/progressive-disclosure/)).
- NN/g, подсказки: не для жизненно важного, не дублируют подпись ([nngroup.com/articles/tooltip-guidelines](https://www.nngroup.com/articles/tooltip-guidelines/)).
- Fluent, панель команд и кнопки: подпись коротким словом, одна-две кнопки на виду ([learn.microsoft.com/…/controls/command-bar](https://learn.microsoft.com/en-us/windows/apps/design/controls/command-bar)).
- Open Design 0.19.2: `craft/anti-ai-slop.md`, `craft/state-coverage.md`, `craft/laws-of-ux.md`.
- Эталон поведения: навык Open Design `brand-extract`. Страница бренд-кита открывается сразу с первой отрисовкой и дозаполняется по частям на глазах у человека. У цветов семь смысловых ролей, и каждое значение выведено из замера, а не из догадки.

### Оформление самого сервиса

Файла токенов сервиса нет. Прототип свёрстан в бежево-зелёном стиле рабочей страницы живого режима. Дизайн-система загруженного шаблона оформляет только слайды: в заголовки, марку и подписи интерфейса она не попадает. Какой системой оформлять сам сервис, решает владелец.

### Чего на экране нет

Марки, версии и дат в углах. Объясняющих абзацев под заголовками. Слагов вместо имён. Служебного: `manifest.json`, идентификаторов образцов `p001`, уверенности разбора, хэшей. Счётчиков, которые повторяют фильтры. Кнопок с фразой из трёх слов.

### Направления на выбор

| Направление | Как устроено | Сильная сторона |
|---|---|---|
| А. Разбор по частям | одна страница сверху вниз: паспорт, палитра, шрифты и шкала, поля, образцы; части проявляются по мере готовности, как бренд-кит Open Design | создание дизайн-системы видно как процесс, это и есть показ для жюри |
| Б. Образцы в центре | главное место у сетки слайдов-образцов: из них сервис верстает; токены в узкой панели справа; клик по образцу показывает его слоты | сразу видно, какие макеты пойдут в колоду |
| В. Три шага | загрузка, проверка (подтвердить палитру, шрифт, образцы), готово; после создания страница как в А | самый короткий путь для нового автора |

## Экраны 2 и 3

«Колода» и «Сцена» работают прототипом, их состав описан в `docs/design/ui-requirements.md`. Брифы на них пишутся после того, как первый экран принят. Что уже известно:

- на «Колоде» выбор системы ведёт на список «Дизайн-системы», а не открывает свой выбор с загрузкой;
- на «Сцене» система выбирается по имени, а не по слагу.

## Вопросы владельцу

1. Навигация: «Дизайн-системы», «Колода», «Сцена»; вкладку «Речь» убрать?
2. Состав экрана «Дизайн-система» из девяти строк выше: подтверждаете? Отдельно: нужно ли исключать образцы из вёрстки (строка 7, этого механизма в сервисе пока нет)?
3. Оформление самого сервиса: оставить бежево-зелёное, взять систему из Open Design или другое?
4. Направление экрана: А, Б или В?
